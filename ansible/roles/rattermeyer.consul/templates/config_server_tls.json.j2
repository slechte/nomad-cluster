{
  "ports": {
    "https": {{ consul_https_port }}
  },
  "ca_file": "{{ consul_tls_dest_dir }}/{{ consul_tls_ca_crt }}",
  "cert_file": "{{ consul_tls_dest_dir }}/{{ consul_tls_crt }}",
  "key_file": "{{ consul_tls_dest_dir }}/{{ consul_tls_key }}",
  "verify_incoming": {{ consul_verify_incoming | to_json }},
  "verify_outgoing": {{ consul_verify_outgoing | to_json }}
}
